#include <stdio.h>

int main() {
    int Arr[5] = {12, 56, 34, 78, 100};
    int i;

    // Print the array elements
    for(i = 0; i < 5; i++) {
        printf("Arr[%d] = %d\n", i, Arr[i]);
    }

    return 0;
}
